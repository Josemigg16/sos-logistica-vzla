meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "username": "coordinador_guanare",
    "password": "changeme123",
    "role": "HUB_COORDINATOR",
    "email": "guanare@sos.org"
  }
}
